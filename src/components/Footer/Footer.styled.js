import styled from "styled-components";

export const StyledFooter = styled.footer`
  border-top: 1px solid #999;
  align-self: bottom;
`;

export const PricacyWrapper = styled.div`
  ul {
    display: flex;
    flex-wrap: wrap;
    list-style: none;

    li {
      color: #999;
      margin-right: 10px;
    }
  }
`;
