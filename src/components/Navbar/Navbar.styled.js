import styled from "styled-components";

export const NavWrapper = styled.nav`
  border-bottom: 1px solid #999;

  ul {
    display: flex;
    flex-wrap: wrap;
    list-style: none;

    li {
      color: black;
      margin-right: 10px;
    }
  }
`;
